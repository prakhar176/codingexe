def check_prime(n):
    for i in range(2,n):
         if n % i == 0:
             return "Not prime"
        
    else:
        return "Prime"


for i in range(2,100):
    out = check_prime(i)
    print(i,out)



    ##### hw = waf to add & display all the odd numbers from start to end
    ###### values given as parmeters
    ###### waf to count all the words , lines and charachters in a given string passed as parameter
    ###### waf to find the longest word in a given string passed as parameter